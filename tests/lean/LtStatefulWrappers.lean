-- THIS FILE WAS AUTOMATICALLY GENERATED BY AENEAS
-- [lt_stateful_wrappers]
import Aeneas
open Aeneas Aeneas.Std Result ControlFlow Error
set_option linter.dupNamespace false
set_option linter.hashCommand false
set_option linter.unusedVariables false

/- You can set the `maxHeartbeats` value with the `-max-heartbeats` CLI option -/
set_option maxHeartbeats 1000000

/- You can set the `maxRecDepth` value with the `-max-recdepth` CLI option -/
set_option maxRecDepth 2048

/- You can remove the following line by using the CLI option `-all-computable`: -/
noncomputable section

namespace lt_stateful_wrappers

/-- [alloc::sync::Arc]
    Source: '/rustc/library/alloc/src/sync.rs', lines 269:0-272:1
    Name pattern: [alloc::sync::Arc]
    Visibility: public -/
@[rust_type "alloc::sync::Arc"]
axiom alloc.sync.Arc (T : Type) : Type

/-- [alloc::sync::{impl core::ops::deref::Deref<T> for alloc::sync::Arc<T>}::deref]:
    Source: '/rustc/library/alloc/src/sync.rs', lines 2443:4-2443:25
    Name pattern: [alloc::sync::{core::ops::deref::Deref<alloc::sync::Arc<@T>, @T>}::deref]
    Visibility: public -/
@[rust_fun
  "alloc::sync::{core::ops::deref::Deref<alloc::sync::Arc<@T>, @T>}::deref"]
axiom alloc.sync.Arc.Insts.CoreOpsDerefDeref.deref
  {T : Type} (A : Type) : alloc.sync.Arc T → Result T

/-- [lt_stateful_wrappers::Lock]
    Source: 'tests/src/lt-stateful-wrappers.rs', lines 58:0-60:1
    Visibility: public -/
axiom Lock (T : Type) : Type

/-- [lt_stateful_wrappers::ReadGuard]
    Source: 'tests/src/lt-stateful-wrappers.rs', lines 63:0-65:1
    Visibility: public -/
axiom ReadGuard (T : Type) : Type

/-- [lt_stateful_wrappers::WriteGuard]
    Source: 'tests/src/lt-stateful-wrappers.rs', lines 68:0-70:1
    Visibility: public -/
axiom WriteGuard (T : Type) : Type

/-- [lt_stateful_wrappers::{lt_stateful_wrappers::Lock<T>}::new]:
    Source: 'tests/src/lt-stateful-wrappers.rs', lines 74:4-76:5
    Visibility: public -/
axiom Lock.new {T : Type} : T → Result (Lock T)

/-- [lt_stateful_wrappers::{lt_stateful_wrappers::Lock<T>}::read]:
    Source: 'tests/src/lt-stateful-wrappers.rs', lines 79:4-81:5
    Visibility: public -/
axiom Lock.read {T : Type} : Lock T → Result (ReadGuard T)

/-- [lt_stateful_wrappers::{lt_stateful_wrappers::Lock<T>}::write]:
    Source: 'tests/src/lt-stateful-wrappers.rs', lines 84:4-86:5
    Visibility: public -/
axiom Lock.write
  {T : Type} : Lock T → Result ((WriteGuard T) × (WriteGuard T → Unit))

/-- [lt_stateful_wrappers::{lt_stateful_wrappers::ReadGuard<'a, T>}::get]:
    Source: 'tests/src/lt-stateful-wrappers.rs', lines 91:4-93:5
    Visibility: public -/
axiom ReadGuard.get {T : Type} : ReadGuard T → Result T

/-- [lt_stateful_wrappers::{lt_stateful_wrappers::ReadGuard<'a, T>}::release]:
    Source: 'tests/src/lt-stateful-wrappers.rs', lines 96:4-96:27
    Visibility: public -/
axiom ReadGuard.release {T : Type} : ReadGuard T → Result Unit

/-- [lt_stateful_wrappers::{lt_stateful_wrappers::WriteGuard<'a, T>}::get]:
    Source: 'tests/src/lt-stateful-wrappers.rs', lines 101:4-103:5
    Visibility: public -/
axiom WriteGuard.get {T : Type} : WriteGuard T → Result T

/-- [lt_stateful_wrappers::{lt_stateful_wrappers::WriteGuard<'a, T>}::get_mut]:
    Source: 'tests/src/lt-stateful-wrappers.rs', lines 106:4-108:5
    Visibility: public -/
axiom WriteGuard.get_mut
  {T : Type} :
  WriteGuard T → Result (T × (T → WriteGuard T) × (WriteGuard T →
    WriteGuard T))

/-- [lt_stateful_wrappers::{lt_stateful_wrappers::WriteGuard<'a, T>}::release]:
    Source: 'tests/src/lt-stateful-wrappers.rs', lines 111:4-111:27
    Visibility: public -/
axiom WriteGuard.release {T : Type} : WriteGuard T → Result (WriteGuard T)

/-- [lt_stateful_wrappers::Pair]
    Source: 'tests/src/lt-stateful-wrappers.rs', lines 115:0-118:1
    Visibility: public -/
structure Pair where
  a : Std.I32
  b : Std.I32

/-- [lt_stateful_wrappers::Cache]
    Source: 'tests/src/lt-stateful-wrappers.rs', lines 125:0-127:1
    Visibility: public -/
structure Cache (T : Type) where
  inner : Lock T

/-- [lt_stateful_wrappers::{lt_stateful_wrappers::Cache<T>}::new]:
    Source: 'tests/src/lt-stateful-wrappers.rs', lines 131:4-135:5
    Visibility: public -/
axiom Cache.new {T : Type} : T → Result (Cache T)

/-- [lt_stateful_wrappers::{lt_stateful_wrappers::Cache<T>}::acquire_write]:
    Source: 'tests/src/lt-stateful-wrappers.rs', lines 142:4-144:5
    Visibility: public -/
def Cache.acquire_write
  {T : Type} (self : Cache T) :
  Result ((WriteGuard T) × (WriteGuard T → Unit))
  := do
  let (wg, _) ← Lock.write self.inner
  let back := fun wg1 => ()
  ok (wg, back)

/-- [lt_stateful_wrappers::{lt_stateful_wrappers::Cache<T>}::acquire_write_named]:
    Source: 'tests/src/lt-stateful-wrappers.rs', lines 150:4-152:5
    Visibility: public -/
def Cache.acquire_write_named
  {T : Type} (self : Cache T) :
  Result ((WriteGuard T) × (WriteGuard T → Unit))
  := do
  let (wg, _) ← Lock.write self.inner
  let back := fun wg1 => ()
  ok (wg, back)

/-- [lt_stateful_wrappers::acquire_generic]:
    Source: 'tests/src/lt-stateful-wrappers.rs', lines 163:0-165:1
    Visibility: public -/
def acquire_generic
  {T : Type} (lock : Lock T) :
  Result ((WriteGuard T) × (WriteGuard T → Unit))
  := do
  let (wg, _) ← Lock.write lock
  let back := fun wg1 => ()
  ok (wg, back)

/-- [lt_stateful_wrappers::acquire_generic_bound]:
    Source: 'tests/src/lt-stateful-wrappers.rs', lines 171:0-173:1
    Visibility: public -/
def acquire_generic_bound
  {T : Type} (coredefaultDefaultInst : core.default.Default T) (lock : Lock T)
  :
  Result ((WriteGuard T) × (WriteGuard T → Unit))
  := do
  let (wg, _) ← Lock.write lock
  let back := fun wg1 => ()
  ok (wg, back)

/-- [lt_stateful_wrappers::{impl lt_stateful_wrappers::ReadLockable<T> for lt_stateful_wrappers::Cache<T>}::acquire_read]:
    Source: 'tests/src/lt-stateful-wrappers.rs', lines 214:4-216:5
    Visibility: public -/
def Cache.Insts.Lt_stateful_wrappersReadLockable.acquire_read
  {T : Type} (self : Cache T) : Result (ReadGuard T) := do
  Lock.read self.inner

/-- [lt_stateful_wrappers::acquire_two_via_wrappers]:
    Source: 'tests/src/lt-stateful-wrappers.rs', lines 181:0-192:1
    Visibility: public -/
def acquire_two_via_wrappers
  {T : Type} {U : Type} (coremarkerCopyInst : core.marker.Copy T)
  (coremarkerCopyInst1 : core.marker.Copy U) (cache1 : Cache T)
  (cache2 : Cache U) :
  Result (T × U)
  := do
  let g1 ← Cache.Insts.Lt_stateful_wrappersReadLockable.acquire_read cache1
  let g2 ← Cache.Insts.Lt_stateful_wrappersReadLockable.acquire_read cache2
  let v1 ← ReadGuard.get g1
  let v2 ← ReadGuard.get g2
  ReadGuard.release g1
  ReadGuard.release g2
  ok (v1, v2)

/-- Trait declaration: [lt_stateful_wrappers::ReadLockable]
    Source: 'tests/src/lt-stateful-wrappers.rs', lines 199:0-208:1
    Visibility: public -/
structure ReadLockable (Self : Type) (Self_Item : Type) where
  acquire_read : Self → Result (ReadGuard Self_Item)

/-- Trait implementation: [lt_stateful_wrappers::{impl lt_stateful_wrappers::ReadLockable<T> for lt_stateful_wrappers::Cache<T>}]
    Source: 'tests/src/lt-stateful-wrappers.rs', lines 210:0-217:1 -/
@[reducible]
def Cache.Insts.Lt_stateful_wrappersReadLockable (T : Type) : ReadLockable
  (Cache T) T := {
  acquire_read := Cache.Insts.Lt_stateful_wrappersReadLockable.acquire_read
}

/-- Trait declaration: [lt_stateful_wrappers::Lockable]
    Source: 'tests/src/lt-stateful-wrappers.rs', lines 220:0-231:1
    Visibility: public -/
structure Lockable (Self : Type) (Self_Item : Type) where
  lock_ref : Self → Result (Lock Self_Item)
  acquire_write_default : Self → Result ((WriteGuard Self_Item) ×
    (WriteGuard Self_Item → Unit))

/-- [lt_stateful_wrappers::Lockable::acquire_write_default]:
    Source: 'tests/src/lt-stateful-wrappers.rs', lines 228:4-230:5
    Visibility: public -/
@[trait_default]
def Lockable.acquire_write_default.default
  {Self : Type} {Clause0_Item : Type} (LockableInst : Lockable Self
  Clause0_Item) (self : Self) :
  Result ((WriteGuard Clause0_Item) × (WriteGuard Clause0_Item → Unit))
  := do
  let l ← LockableInst.lock_ref self
  let (wg, _) ← Lock.write l
  let back := fun wg1 => ()
  ok (wg, back)

/-- [lt_stateful_wrappers::{impl lt_stateful_wrappers::Lockable<T> for lt_stateful_wrappers::Cache<T>}::lock_ref]:
    Source: 'tests/src/lt-stateful-wrappers.rs', lines 239:4-241:5
    Visibility: public -/
def Cache.Insts.Lt_stateful_wrappersLockable.lock_ref
  {T : Type} (self : Cache T) : Result (Lock T) := do
  ok self.inner

/-- Trait implementation: [lt_stateful_wrappers::{impl lt_stateful_wrappers::Lockable<T> for lt_stateful_wrappers::Cache<T>}]
    Source: 'tests/src/lt-stateful-wrappers.rs', lines 233:0-242:1 -/
@[reducible]
impl_def Cache.Insts.Lt_stateful_wrappersLockable (T : Type) : Lockable (Cache
  T) T := {
  lock_ref := Cache.Insts.Lt_stateful_wrappersLockable.lock_ref
  acquire_write_default := Lockable.acquire_write_default.default
    (Cache.Insts.Lt_stateful_wrappersLockable T)
}

/-- [lt_stateful_wrappers::Vault]
    Source: 'tests/src/lt-stateful-wrappers.rs', lines 249:0-251:1
    Visibility: public -/
structure Vault (T : Type) where
  guarded : Lock T

/-- [lt_stateful_wrappers::{impl lt_stateful_wrappers::ReadLockable<T> for lt_stateful_wrappers::Vault<T>}::acquire_read]:
    Source: 'tests/src/lt-stateful-wrappers.rs', lines 261:4-263:5
    Visibility: public -/
def Vault.Insts.Lt_stateful_wrappersReadLockable.acquire_read
  {T : Type} (self : Vault T) : Result (ReadGuard T) := do
  Lock.read self.guarded

/-- Trait implementation: [lt_stateful_wrappers::{impl lt_stateful_wrappers::ReadLockable<T> for lt_stateful_wrappers::Vault<T>}]
    Source: 'tests/src/lt-stateful-wrappers.rs', lines 253:0-264:1 -/
@[reducible]
def Vault.Insts.Lt_stateful_wrappersReadLockable (T : Type) : ReadLockable
  (Vault T) T := {
  acquire_read := Vault.Insts.Lt_stateful_wrappersReadLockable.acquire_read
}

/-- [lt_stateful_wrappers::via_inherent_wrapper_then_release]:
    Source: 'tests/src/lt-stateful-wrappers.rs', lines 271:0-276:1
    Visibility: public -/
def via_inherent_wrapper_then_release
  (cache : Cache Std.I32) : Result Std.I32 := do
  let (guard, _) ← Cache.acquire_write cache
  let value ← WriteGuard.get guard
  let _ ← WriteGuard.release guard
  ok value

/-- [lt_stateful_wrappers::via_trait_method_then_release]:
    Source: 'tests/src/lt-stateful-wrappers.rs', lines 279:0-284:1
    Visibility: public -/
def via_trait_method_then_release
  (cache : Cache Std.I32) : Result Std.I32 := do
  let guard ← Cache.Insts.Lt_stateful_wrappersReadLockable.acquire_read cache
  let value ← ReadGuard.get guard
  ReadGuard.release guard
  ok value

/-- [lt_stateful_wrappers::via_default_trait_method_then_release]:
    Source: 'tests/src/lt-stateful-wrappers.rs', lines 287:0-291:1
    Visibility: public -/
def via_default_trait_method_then_release
  (cache : Cache Std.I32) : Result Unit := do
  let (guard, _) ←
    Lockable.acquire_write_default.default
      (Cache.Insts.Lt_stateful_wrappersLockable Std.I32) cache
  let (_, get_mut_back, _) ← WriteGuard.get_mut guard
  let guard1 := get_mut_back 7#i32
  let _ ← WriteGuard.release guard1
  ok ()

/-- [lt_stateful_wrappers::via_generic_trait_bound]:
    Source: 'tests/src/lt-stateful-wrappers.rs', lines 294:0-299:1
    Visibility: public -/
def via_generic_trait_bound
  {L : Type} (ReadLockableLI32Inst : ReadLockable L Std.I32) (lockable : L) :
  Result Std.I32
  := do
  let guard ← ReadLockableLI32Inst.acquire_read lockable
  let value ← ReadGuard.get guard
  ReadGuard.release guard
  ok value

/-- [lt_stateful_wrappers::poke_guard]:
    Source: 'tests/src/lt-stateful-wrappers.rs', lines 320:0-322:1
    Visibility: public -/
axiom poke_guard
  {T : Type} :
  WriteGuard T → Result ((WriteGuard T) × (WriteGuard T → WriteGuard T))

/-- [lt_stateful_wrappers::acquire_then_poke]:
    Source: 'tests/src/lt-stateful-wrappers.rs', lines 330:0-334:1
    Visibility: public -/
def acquire_then_poke {T : Type} (lock : Lock T) : Result Unit := do
  let (guard, _) ← Lock.write lock
  let (guard1, _) ← poke_guard guard
  let _ ← WriteGuard.release guard1
  ok ()

/-- [lt_stateful_wrappers::acquire_via_arc]:
    Source: 'tests/src/lt-stateful-wrappers.rs', lines 350:0-352:1
    Visibility: public -/
def acquire_via_arc
  {T : Type} (lock : alloc.sync.Arc (Lock T)) :
  Result ((WriteGuard T) × (WriteGuard T → Unit))
  := do
  let l ← alloc.sync.Arc.Insts.CoreOpsDerefDeref.deref Global lock
  let (wg, _) ← Lock.write l
  let back := fun wg1 => ()
  ok (wg, back)

/-- [lt_stateful_wrappers::acquire_via_arc_read]:
    Source: 'tests/src/lt-stateful-wrappers.rs', lines 357:0-359:1
    Visibility: public -/
def acquire_via_arc_read
  {T : Type} (lock : alloc.sync.Arc (Lock T)) : Result (ReadGuard T) := do
  let l ← alloc.sync.Arc.Insts.CoreOpsDerefDeref.deref Global lock
  Lock.read l

/-- [lt_stateful_wrappers::Cell]
    Source: 'tests/src/lt-stateful-wrappers.rs', lines 363:0-365:1
    Visibility: public -/
structure Cell (T : Type) where
  inner : T

/-- [lt_stateful_wrappers::{impl core::ops::deref::Deref<T> for lt_stateful_wrappers::Cell<T>}::deref]:
    Source: 'tests/src/lt-stateful-wrappers.rs', lines 370:4-372:5
    Visibility: public -/
def Cell.Insts.CoreOpsDerefDeref.deref
  {T : Type} (self : Cell T) : Result T := do
  ok self.inner

/-- Trait implementation: [lt_stateful_wrappers::{impl core::ops::deref::Deref<T> for lt_stateful_wrappers::Cell<T>}]
    Source: 'tests/src/lt-stateful-wrappers.rs', lines 367:0-373:1 -/
@[reducible]
def Cell.Insts.CoreOpsDerefDeref (T : Type) : core.ops.deref.Deref (Cell T) T
  := {
  deref := Cell.Insts.CoreOpsDerefDeref.deref
}

/-- [lt_stateful_wrappers::acquire_via_arc_cell]:
    Source: 'tests/src/lt-stateful-wrappers.rs', lines 384:0-386:1
    Visibility: public -/
def acquire_via_arc_cell
  {T : Type} (lock : alloc.sync.Arc (Cell (Lock T))) :
  Result ((WriteGuard T) × (WriteGuard T → Unit))
  := do
  let c ← alloc.sync.Arc.Insts.CoreOpsDerefDeref.deref Global lock
  let l ← Cell.Insts.CoreOpsDerefDeref.deref c
  let (wg, _) ← Lock.write l
  let back := fun wg1 => ()
  ok (wg, back)

/-- [lt_stateful_wrappers::GuardBox]
    Source: 'tests/src/lt-stateful-wrappers.rs', lines 420:0-422:1
    Visibility: public -/
structure GuardBox (T : Type) where
  guard : WriteGuard T

/-- [lt_stateful_wrappers::wrap_guard]:
    Source: 'tests/src/lt-stateful-wrappers.rs', lines 428:0-430:1
    Visibility: public -/
def wrap_guard
  {T : Type} (guard : WriteGuard T) :
  Result ((GuardBox T) × (GuardBox T → WriteGuard T))
  := do
  let back := fun gb => gb.guard
  ok ({ guard }, back)

/-- [lt_stateful_wrappers::DoubleGuardBox]
    Source: 'tests/src/lt-stateful-wrappers.rs', lines 438:0-440:1
    Visibility: public -/
structure DoubleGuardBox (T : Type) where
  inner : GuardBox T

/-- [lt_stateful_wrappers::wrap_guard_twice]:
    Source: 'tests/src/lt-stateful-wrappers.rs', lines 447:0-451:1
    Visibility: public -/
def wrap_guard_twice
  {T : Type} (guard : WriteGuard T) :
  Result ((DoubleGuardBox T) × (DoubleGuardBox T → WriteGuard T))
  := do
  let back := fun dgb => dgb.inner.guard
  ok ({ inner := { guard } }, back)

/-- [lt_stateful_wrappers::acquire_wrap_use_release]:
    Source: 'tests/src/lt-stateful-wrappers.rs', lines 456:0-461:1
    Visibility: public -/
def acquire_wrap_use_release (lock : Lock Std.I32) : Result Unit := do
  let (guard, _) ← Lock.write lock
  let (boxed, _) ← wrap_guard guard
  let (_, get_mut_back, _) ← WriteGuard.get_mut boxed.guard
  let wg := get_mut_back 9#i32
  let _ ← WriteGuard.release wg
  ok ()

/-- [lt_stateful_wrappers::acquire_via_alias]:
    Source: 'tests/src/lt-stateful-wrappers.rs', lines 479:0-481:1
    Visibility: public -/
def acquire_via_alias
  (lock : Lock Std.I32) :
  Result ((WriteGuard Std.I32) × (WriteGuard Std.I32 → Unit))
  := do
  let (wg, _) ← Lock.write lock
  let back := fun wg1 => ()
  ok (wg, back)

/-- [lt_stateful_wrappers::wrap_guard_via_alias]:
    Source: 'tests/src/lt-stateful-wrappers.rs', lines 490:0-492:1
    Visibility: public -/
def wrap_guard_via_alias
  (guard : WriteGuard Std.I32) :
  Result ((GuardBox Std.I32) × (GuardBox Std.I32 → WriteGuard Std.I32))
  := do
  let back := fun gb => gb.guard
  ok ({ guard }, back)

end lt_stateful_wrappers
