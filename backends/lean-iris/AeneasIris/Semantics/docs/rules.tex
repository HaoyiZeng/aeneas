% Generated by AeneasIris/Semantics/docs/rules.py — do not edit by hand.
% Requires: \usepackage{mathpartir}
\providecommand{\focus}{\mathrm{focus}}
\providecommand{\heap}{\mathrm{heap}}
\providecommand{\cur}{\mathrm{cur}}
\providecommand{\threads}{\mathrm{threads}}
% \kill is already a LaTeX command (tabbing), and \providecommand would
% silently decline to redefine it, so this one is \killt.
\providecommand{\killt}{\mathrm{kill}}
\providecommand{\alive}{\mathrm{alive}}
\providecommand{\alivet}{\mathsf{alive}}
\providecommand{\setidx}{\mathrm{set}}
\providecommand{\dplus}{\mathbin{+\!\!+}}
\providecommand{\stepto}{\longrightarrow}
\providecommand{\stepsto}{\longrightarrow^{*}}
\providecommand{\opstep}{\mathsf{step}}
\providecommand{\opyield}{\mathsf{yield}}
\providecommand{\opfork}{\mathsf{fork}}
\providecommand{\opend}{\mathsf{endthread}}
\providecommand{\opmod}{\mathsf{modify}}
\providecommand{\tagcur}{\mathsf{cur}}
\providecommand{\tagnew}{\mathsf{new}}
\providecommand{\vis}[2]{\mathsf{vis}\;#1\;#2}

% ---- The machine
% Machine.lean, inductive Step
\begin{mathpar}
\inferrule*[left=\textsc{tick}]
  {\focus(c) = \vis{\opstep}{k}}
  {c \stepto c[\focus \mapsto k ()]}

\inferrule*[left=\textsc{heap}]
  {\focus(c) = \vis{\opmod\,f}{k} \\ f \heap(c) = \sigma'}
  {c \stepto c[\focus \mapsto k \heap(c)][\heap \mapsto \sigma']}

\inferrule*[left=\textsc{yield}]
  {\focus(c) = \vis{\opyield}{k} \\ \alive(c[\focus \mapsto k ()], j)}
  {c \stepto c[\focus \mapsto k ()][\cur \mapsto j]}

\inferrule*[left=\textsc{fork}]
  {\focus(c) = \vis{\opfork}{k}}
  {c \stepto c[\threads \mapsto (\threads(c)\,\setidx\,\cur(c) (\alivet\,(k \tagcur))) \dplus [\alivet\,(k \tagnew)]]}

\inferrule*[left=\textsc{endthread}]
  {\focus(c) = \vis{\opend}{k} \\ \alive(\killt(c), j)}
  {c \stepto \killt(c)[\cur \mapsto j]}
\end{mathpar}

% ---- Reachability
% Machine.lean, inductive Reaches
\begin{mathpar}
\inferrule*[left=\textsc{refl}]
  {\ }
  {c \stepsto c}

\inferrule*[left=\textsc{tail}]
  {c_1 \stepto c_2 \\ c_2 \stepsto c_3}
  {c_1 \stepsto c_3}
\end{mathpar}
