# This is the commit from https://github.com/HaoyiZeng/charon that should be used
# with this version of aeneas.
#
# NOTE: this points into a personal fork, not upstream AeneasVerif/charon. The
# stateful-lifetimes support needs the `stateful` bit on `RegionParam`, which
# only exists there. The upstream CI helpers scripts/ci-check-charon-pin-is-*.sh
# check that the pin is merged upstream and will therefore fail; that is
# expected and harmless for this fork.
9229cf0b2470f39fe4e82cfbf2dfeb0b48a0068f
